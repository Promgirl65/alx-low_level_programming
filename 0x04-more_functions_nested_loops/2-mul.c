#include "main.h"
/**
 * mul -  checks for checks for a digit (0 through 9).
 * @a: a -  first variable
 * @b: b - second variable
 * Return: multiplication
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
